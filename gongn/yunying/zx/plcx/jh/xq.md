# BI汇总查询计划模块

- `gz.js`：维护模板合并识别、字段名称归一化和率字段依赖规则。
- `yw.js`：为 BI 汇总查询和指标详解生成最小查询计划。
- BI 汇总查询：普通模板不传查询字段，只查当前显示聚合字段；合并模板不传查询字段，但为率字段补齐计算依赖字段。
- 指标详解：只使用日期字段作为内部分组字段；聚合字段按用户双击字段生成，率字段使用依赖字段前端累计计算。
- 查询计划通过 `context.queryPlan` 传入请求构造层，避免空字段配置被默认字段回填。
