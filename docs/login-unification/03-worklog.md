# 登录统一接入工作记录

## 2026-06-15

### 已完成

- 完成现状初审。
- 确认登录模块与业务模块仍有部分绕行点。
- 确认 BI 需要代理优先、token 后置的特殊顺序。
- 确认后续要收口为单一登录入口。
- 补充了统一登录接口契约与业务接入审计清单。

### 当前结论

- 登录模块应该成为唯一凭证出口。
- 业务模块不应直接碰登录缓存。
- HTML 应只保留一个登录入口脚本。
- 登录依赖应由登录模块内部动态加载。
- 旧接口壳不再保留，最终只保留统一入口和标准错误返回。

### 待办

- 设计 `requireCredentials` 的最终返回结构。
- 收口 `index.html` 的登录脚本加载。
- 收口登录模块内部动态加载。
- 逐业务替换直接凭证读取。
- 清理旧接口和绕行点。
- 逐步建立每个业务模块的验收记录。

### 备注

- 本记录文件将随重构过程持续追加。
- 每完成一个关键点，先检查，再记录，再进入下一点。
