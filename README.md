# 商品查询模块集成说明

## 📦 新增/修改文件清单

### 新建文件
- `yewu/shangpin.js` - 商品查询核心模块

### 修改文件
- `yewu/zhuce.js` - 业务注册中心（添加shangpin模块导入）
- `jiemian/xuanran.js` - 渲染模块（添加商品卡片渲染函数）
- `jiemian/jieguo.css` - 样式（添加商品卡片样式）
- `jiemian/jiaohui.js` - 交互模块（添加showProductDetail方法）
- `qidong.js` - 启动器（添加renderProductCard回调）
- `danaoji/tishici.js` - AI提示词（添加商品查询工具说明）

---

## 🔧 集成步骤

### 1. 复制文件
将对应文件替换到您项目的相应目录：
```
F:/Dpzhuomian/yh-main/
├── qidong.js                    ← 替换
├── yewu/
│   ├── shangpin.js              ← 新增
│   └── zhuce.js                 ← 替换
├── jiemian/
│   ├── jiaohui.js               ← 替换
│   ├── jieguo.css               ← 替换
│   └── xuanran.js               ← 替换
└── danaoji/
    └── tishici.js               ← 替换
```

### 2. 配置SCM凭证
首次使用时，需要配置药师帮SCM登录凭证。用户可以通过以下方式：

**方式一：对话配置**
用户可以直接告诉AI：
```
设置SCM凭证
token: c3ebf6487fc9b2c7bcfc26467c8ad213
cookies: ysbang_adminToken=xxx; cmsToken=xxx; PHPSESSID=xxx
```

**方式二：浏览器手动获取**
1. 登录 https://scm.ysbang.cn
2. F12打开开发者工具 → Application → Cookies
3. 复制 `ysbang_adminToken` 值作为token
4. 复制所有Cookie拼接作为cookies

---

## 🎯 功能说明

### AI识别的查询意图
- "查一下三金片"
- "三金片多少钱"
- "阿莫西林有货吗"
- "SP0247788价格" （商品编码）
- "国药准字Z45021645" （批准文号）

### 查询类型
| 用户说法 | wholesaleType | 说明 |
|---------|---------------|------|
| 默认 | 1 | 一口价 |
| "特价" | 2 | 特价 |
| "限时特价" | 3 | 限时特价 |
| "全部类型" | 0 | 所有类型 |

### 商品卡片显示
```
┌─────────────────────────────────────┐
│ 4007294 │ SP0247788      [详情]     │
├─────────────────────────────────────┤
│ 三金 三金片 0.32g*54片(OTC)          │
├─────────────────────────────────────┤
│ 单体价格          ¥22.40            │
│ 一环价            ¥22.40            │
│ 省内价            ¥22.40            │
│ 周边省份价        -                 │
│ 连锁价格          ¥22.40            │
├─────────────────────────────────────┤
│ 成本：¥21.43      库存：5315        │
│ 有效期：2028-09-17                  │
│ 桂林三金药业股份有限公司             │
└─────────────────────────────────────┘
```

### 详情弹窗
点击"详情"按钮可查看：
- 所有匹配的活动记录（可下滑查看多条）
- 每条记录的完整信息：
  - 基本信息（商品ID、编码、批准文号等）
  - 价格信息（6种价格）
  - 库存信息
  - 活动信息（类型、商圈、仓库等）
  - 销售统计

---

## 📝 API说明

### 云函数地址
```
POST https://1317825751-lqnvz24xzp.ap-guangzhou.tencentscf.com
```

### 请求参数
```json
{
  "auth": {
    "token": "string, 必填",
    "cookies": "string, 必填"
  },
  "query": {
    "keyword": "搜索关键词",
    "wholesaleType": 1,   // 0=全部, 1=一口价, 2=特价...
    "pageSize": 50,
    "fetchPages": 1
  }
}
```

### 凭证存储
凭证保存在 `localStorage`，key 为 `ysbang_scm_auth`。

---

## ⚠️ 注意事项

1. **凭证有效期**：Token会过期，过期后需重新配置
2. **网络要求**：需要能访问腾讯云函数（无特殊网络限制）
3. **默认行为**：默认查询进行中的一口价商品
4. **兼容性**：不影响原有的优惠券发放功能

---

## 🐛 常见问题

**Q: 提示"未配置SCM凭证"？**
A: 需要先配置凭证，参见上方"配置SCM凭证"章节

**Q: 提示"登录已过期"？**
A: Token已失效，需要重新登录SCM后台获取新凭证

**Q: 查询结果为空？**
A: 检查关键词是否正确，或该商品确实未上架

---

## 📄 完整文件对照

如果需要对比具体修改了哪些内容：

### yewu/zhuce.js 主要修改
```javascript
// 新增导入
import * as shangpin from './shangpin.js';

// modules中添加
const modules = { ..., shangpin };
```

### jiemian/jiaohui.js 主要修改
```javascript
// 新增导入
import { renderProductCard, renderProductDetailContent } from './xuanran.js';

// 新增全局方法
window.showProductDetail = id => { ... };

// 新增导出函数
export function addProductCard(product, allProducts) { ... }
```

### qidong.js 主要修改
```javascript
// 新增回调函数
function renderProductCard(product, allProducts) {
  jiemian.addProductCard(product, allProducts);
}

// getSendContext中添加
renderProductCard,
```
